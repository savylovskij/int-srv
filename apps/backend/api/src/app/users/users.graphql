extend type Query{
    user(id: Int): User
}

type User {
    id: Int
    username: String
    password: String
    created: Date
    udated: Date
    email: String
    nickname: String
    phone: String
}
